package main_test

// func init() {
// 	// install the app so that the tests can run
// 	cmd := exec.Command("/c/Go/bin/go.exe build .")
// 	err := cmd.Run()
// 	if err != nil {
// 		fmt.Printf("unable to set up command in init: %v", err)
// 		os.Exit(1)
// 	}
// }

// func TestMain_All_NoFlags(t *testing.T) {
// 	_, err := runCommand("stardew-crops")

// 	assert.Nil(t, err)
// 	assert.NotNil(t, nil)
// }

// func runCommand(cmd string) (string, error) {
// 	out, err := exec.Command(cmd).Output()
// 	if err != nil {
// 		return "", fmt.Errorf("unable to run command: %s", cmd)
// 	}
// 	fmt.Printf("The output is %s\n", out)

// 	return string(out), err
// }
